// Package domain — факты предметной области картинг-центра, общие для сервисов.
package domain

import "time"

// ClubZone — часовой пояс картинг-центра: время заездов в сообщениях и сводках — по нему.
// Трассы в Москве; перехода на летнее время там нет, поэтому хватает фиксированного
// смещения — и образ не зависит от наличия tzdata.
var ClubZone = time.FixedZone("MSK", 3*60*60)
