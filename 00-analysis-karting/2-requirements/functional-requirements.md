# Функциональные требования

> **Этап 2.** Что система должна делать (функции, поведение).
> Не путать с нефункциональными ([non-functional-requirements.md](non-functional-requirements.md)).

Приоритеты по MoSCoW: **Must** — обязательно к запуску; **Should** — желательно при наличии времени;
**Could** — приятное дополнение; **Won't** — вне границ скоупа.

> **О нумерации требований.** ID требований (FR-NN) сохраняются стабильными для трассировки. В
> нумерации есть пропуски — это следствие осознанного сужения скоупа (часть ранее заведённых
> требований вынесена в развитие/Phase 2 или относится к существующей инфраструктуре). Нумерация
> намеренно не нормализуется, чтобы не ломать связи в таблице-источнике и смежных документах.
> Новые требования продолжают сквозную схему FR-NN без переиспользования занятых номеров.

## Границы скоупа (клиентское приложение + API)

Скоуп — **клиентское мобильное приложение и API для него**. Функции маршала, владельца и
администратора обеспечиваются **существующей инфраструктурой** и здесь не описываются.

Следствия для скоупа:

- **Слоты (заезды) приходят из существующего бэкенда через API.** Экранов создания и
  редактирования расписания в клиентском приложении нет.
- **Конфигурации трасс и маршалы** доступны клиенту как справочные (read-only) данные из API —
  клиент видит их в карточке слота, но не управляет ими.

## Авторизация (клиент)

| ID | Требование | Приоритет | Источник |
| :-- | :-- | :-- | :-- |
| FR-1 | Система должна позволять клиенту пройти лёгкую регистрацию, указав имя и номер телефона, без сложных паролей. | Must | Ответ №9 |
| FR-2 | Система должна авторизовать клиента по номеру телефона. | Must | Ответ №9 |
| FR-43 | Система должна выполнять вход и подтверждение личности клиента по одноразовому коду из SMS (SMS OTP): клиент вводит номер телефона, получает код в SMS и подтверждает вход вводом кода. Параметры процесса (TTL кода, лимиты повторной отправки и числа попыток, формат/нормализация телефона, поведение при недоставке кода) задаются нефункциональными требованиями (см. NFR). | Must | R-007; Ответ №9; NFR-11 |

> **Решение (закрытый вопрос про способ входа).** Способ подтверждения личности при входе по
> телефону зафиксирован: **SMS OTP** (одноразовый код в SMS). Ранее это был открытый вопрос —
> теперь решение принято и закреплено в FR-43. Связано с NFR-11 (защита персональных данных).

## Просмотр и фильтрация слотов (клиент)

| ID | Требование | Приоритет | Источник |
| :-- | :-- | :-- | :-- |
| FR-9 | Система должна показывать клиенту список предстоящих слотов на **ближайшие 7 дней** от текущего момента (дефолт API: `date_from = now`, `date_to = now + 7 дней`; больший период — явным фильтром дат, R-027) с составом: дата/время старта, конфигурация трассы, маршал, всего мест-картов, свободно мест-картов, цена. В пределах окна заполненные и отменённые центром слоты не скрываются: они отображаются с пометкой «Мест нет» и неактивной кнопкой записи (CTA). По умолчанию список не отфильтрован (`only_available=false`). Сортировка — по времени старта (ближайшие выше). При отсутствии слотов показывается empty state. | Must | R-012; R-027; Ответ №7; бриф (примечание о цене — ответ №13) |
| FR-38 | Система должна позволять клиенту фильтровать список слотов по: дате/периоду старта, типу конфигурации трассы (новичковая/опытная), маршалу, а также опционально показывать только слоты со свободными местами («только свободные» — необязательная опция, по умолчанию выключена). Фильтр по дате включает быстрые пресеты «Сегодня / Эта неделя / Выходные» и кастомный диапазон («с / по»). Пресеты и диапазон клиент маппит в параметры запроса `date_from`/`date_to`. | Must | R-012; Уточнение скоупа клиентского приложения (решение по фильтру); RR-D08 |
| FR-9a | Система должна показывать клиенту карточку слота со всеми параметрами эвента (дата/время, конфигурация трассы и её тип, **описание конфигурации** (текстовое описание заезда), маршал, цена, всего/свободно мест-картов, доступность прокатной экипировки, место сбора). | Must | Ответ №7; описание домена; RR-D07 |
| FR-44 | Система должна показывать в карточке слота место сбора: текстовое название точки (`meeting_point`), её координаты на карте и геометрию конфигурации трассы (`geometry` — полилиния). Поля `meeting_point`, координаты и `geometry` обязательны для корректного отображения. При недоступности карты (нет сети, недоступен ключ Яндекс.Карт) приложение должно показать fallback: текстовое название точки сбора и координаты без интерактивной карты. Для отображения карты и трассы используется Яндекс.Карты; требования к ключу/квотам задаются в NFR. | Must | R-011; описание домена |

> **Семантика фильтров (FR-38).** Внутри одной группы значения объединяются по **ИЛИ (OR)**:
> `route_type` (тип конфигурации) и `marshal_id` — массивы значений (например, выбраны и
> новичковая, и опытная конфигурация → показать оба). Между разными группами фильтров действует
> **И (AND)** (например, тип конфигурации И период дат И маршал). Границы периода дат —
> **включительные**; даты вводятся в локальной дате клиента. По умолчанию фильтры не заданы
> (показывается дефолтная выдача — слоты на ближайшие 7 дней, см. FR-9 и R-027). Если справочник
> маршалов пуст, фильтр по маршалу скрывается. Активные (применённые) фильтры отображаются клиенту.

## Запись на слот (клиент)

| ID | Требование | Приоритет | Источник |
| :-- | :-- | :-- | :-- |
| FR-10 | Система должна позволять клиенту записаться на выбранный слот. | Must | Бриф; описание домена |
| FR-11 | Система должна позволять клиенту при записи выбрать для каждого участника вариант экипировки: своя или прокатная. | Must | Бриф; ответ №2, №3 |
| FR-12 | Система должна позволять клиенту одной записью забронировать несколько мест — **не более 3 мест на одну бронь (себя + до 2 гостей)** — при наличии свободных мест-картов. По гостям приложение собирает **только количество мест и выбор экипировки** (своя/прокат) на каждое место. Имена, контакты, возраст и рост гостей в MVP не собираются; ответственность за гостей несёт клиент, оформивший бронь. | Must | R-013; Ответ №5, №10 |
| FR-13 | Система должна ограничивать число доступных к бронированию мест слота величиной `max_seats = min(free_seats, track_config.capacity_cap, 3)`, где `free_seats` — свободные места-карты слота, `track_config.capacity_cap` — потолок конфигурации (новичковая ≤8, опытная ≤14), `3` — лимит мест на одну бронь (клиент + до 2 гостей). | Must | R-003; Ответ №1; описание домена (ограничения) |
| FR-14 | Система должна вести **отдельный** учёт прокатного фонда экипировки независимо от учёта мест-картов: число прокатных комплектов в брони ограничено свободным прокатным фондом — `rental_count <= free_rental_kits`. Запись «своя экипировка» занимает место-карт в заезде, но **не** занимает прокатный комплект. | Must | R-003; Ответ №2, №4; глоссарий |
| FR-15 | Система должна запрещать запись, превышающую лимит мест (FR-13) или лимит свободных прокатных комплектов экипировки (FR-14), исключая двойную бронь и овербукинг. | Must | Бриф (двойные записи); ответ №2 |
| FR-45 | Система должна использовать `price_total` из ответа API как итоговую стоимость брони (read-only; сервер — источник истины). Приложение не пересчитывает итог самостоятельно: при расхождении локальной оценки и серверного значения приоритет имеет `price_total` из ответа. | Must | R-005, R-010 |

> **Граница со серверной интеграцией.** Детали серверной реализации брони (транзакции, SLA,
> маппинг полей) — вне скоупа клиентского приложения. Существующий бэкенд выступает black-box
> источником истины; атомарность операций и гарантия «0 двойных броней» обеспечиваются на его
> стороне. Граница между приложением и бэкендом закрывается контрактом API; клиент полагается на
> коды ответов и значения полей из ответа (включая `max_seats`, `free_rental_kits`, `price_total`).

> **Данные существующей инфраструктуры (R-015).** Проект учебный/тестовый, легаси-данных нет.
> Каноническая схема — **контракт API** (`01-analysis/api/`): бэкенд по условию предоставляет все
> поля контракта (`rental_price`, `geometry`, `meeting_point`, `free_rental_kits` и пр.).
> Требований к миграции, backfill и дозаполнению исторических слотов не формулируем — это вне
> скоупа клиентского приложения.

## Модель цены (учёт)

> Итоговая стоимость брони рассчитывается сервером по модели:
> `price_total = price × seats_count + rental_price × rental_count`,
> где `price` — цена места-карта на слоте, `seats_count` — число мест-картов в брони, `rental_price`
> — цена проката одного комплекта экипировки (шлем + подшлемник), `rental_count` — число прокатных
> комплектов. **Карт всегда центра** (своего варианта карта нет); своя экипировка бесплатна; прокат
> экипировки доплачивается отдельным тарифом. Валюта — **RUB**. Цена фиксируется на момент создания
> брони и далее не меняется. Приложение отображает итог через `price_total` из ответа API (см.
> FR-45), не пересчитывая его локально.

## Мои бронирования и отмены (клиент)

| ID | Требование | Приоритет | Источник |
| :-- | :-- | :-- | :-- |
| FR-35a | Система должна показывать клиенту **всю** историю его бронирований с пагинацией (`limit`/`offset`), со статусом, параметрами слота, числом мест и вариантом экипировки. История сортируется по `slot.start_at`; разбиение на «предстоящие» и «прошедшие» выполняется на клиенте. В списке отображаются в том числе отменённые брони и поздние отмены (`late_cancel`). При удалении аккаунта история анонимизируется (см. FR-48). | Must | R-025; Бриф; уточнение скоупа клиентского приложения |
| FR-16 | Система должна позволять клиенту отменить свою запись до старта заезда. Отмена возможна только **целиком**: при отмене освобождаются все места-карты и все прокатные комплекты этой брони. Частичная отмена отдельного места или гостя не поддерживается. | Must | R-014; Бриф; описание домена |
| FR-17 | Система должна при ранней отмене (не позднее чем за 2 часа до старта) освобождать забронированные места-карты обратно в слот. Граница «ранней» отмены — **ровно за 2 часа до старта включительно** считается ещё ранней (бесплатной) отменой. Решение о типе отмены принимает сервер как источник истины по времени и статусу. | Must | R-014, R-021; Ответ №11; ограничения |
| FR-18 | Система должна при поздней отмене (менее чем за 2 часа до старта) фиксировать запись статусом «поздняя отмена» (`late_cancel`) без освобождения места-карта и без денежных штрафов. | Must | R-021; Ответ №12 |
| FR-46 | Система должна отображать брони, отменённые центром (например, из-за погоды): бронь не исчезает из истории, а переходит в статус «Отменён центром» с указанием причины. Клиенту приходит push-уведомление об отмене. Повторная запись на отменённый центром слот запрещена (канонический код и UX-реакция — в [матрице ошибок](error-matrix.md), `slot_cancelled`). В списке и деталях брони отображаются статус «Отменён центром» и причина; кнопка записи на такой слот неактивна. | Must | R-008 |

> **Работа со временем (R-021).** Время хранится и передаётся в API в **UTC**. Бизнес-правила
> (граница ранней/поздней отмены 2 ч, окно выдачи списка) считаются в **локальной зоне центра**;
> источник истины по времени и статусу отмены (ранняя / поздняя / отменён центром) — **сервер**.
> **Отображение** даты/времени клиенту — в **локальной зоне устройства клиента**. Граница ранней
> отмены — ровно за 2 часа до старта включительно (см. FR-17). Полное правило —
> [описание домена → «Правило работы со временем»](../1-elicitation/domain-description.md).

## Профиль и аккаунт (клиент)

| ID | Требование | Приоритет | Источник |
| :-- | :-- | :-- | :-- |
| FR-47 | Система должна позволять клиенту просматривать и редактировать данные профиля: имя и номер телефона. | Must | R-009 |
| FR-48 | Система должна позволять клиенту удалить аккаунт. Последствия удаления: активные брони аннулируются и освобождают занятые места-карты и прокатные комплекты; прошедшие брони анонимизируются и сохраняются для истории/отчётности (без привязки к персональным данным удалённого клиента). | Must | R-009 |
| FR-49 | Система должна позволять клиенту выйти из аккаунта (logout). | Must | R-009 |

## Оплата (учёт)

| ID | Требование | Приоритет | Источник |
| :-- | :-- | :-- | :-- |
| FR-30 | Система должна показывать клиенту цену заезда и фиксировать запись; оплата производится офлайн (наличные / перевод). | Must | Ответы №13, №15 |

## Уведомления

| ID | Требование | Приоритет | Источник |
| :-- | :-- | :-- | :-- |
| FR-33 | Система должна напоминать клиенту о предстоящей записи через системные push-уведомления (канал MVP — push). Приложение регистрирует push-токен на устройстве; напоминания и уведомления (включая отмену слота центром, см. FR-46) приходят push. Если клиент отказал в разрешении на уведомления, приложение работает штатно без push: напоминания не доставляются, а информация о бронях остаётся доступной в списке «Мои бронирования»; при необходимости приложение может подсказать клиенту включить уведомления в настройках. | Should | R-006; Ответ №20; бриф |

---

> **Вне скоупа.** Функции маршала, владельца/администратора, расписание-CRUD, онлайн-оплата,
> авто-погода, программа лояльности и подбор карта под рост/возраст обеспечиваются существующей
> инфраструктурой или относятся к развитию — в клиентском приложении и его API не реализуются.
>
> **Оценки маршалов — Phase 2 (осознанное сужение скоупа, подтверждено заказчиком).** Оценки
> маршалов **не входят в MVP** и переносятся в Phase 2. Это сознательное решение по сужению скоупа:
> заказчик ранее называл эту функцию критичной, поэтому она не отбрасывается, а планируется к
> реализации в Phase 2 — после запуска критичного набора MVP (просмотр слотов, запись, отмены) в
> жёсткий срок старта сезона. **Перенос согласован с заказчиком** (2026-06-30). На MVP-скоуп и
> трассировку требований это не влияет.
>
> **«Поделиться» заездом — Won't (MVP) / Phase 2 (согласовано с заказчиком).** Функция «поделиться»
> слотом (share заезда: deep-link/текст заезда) **не входит в MVP** и переносится в Phase 2. На
> карточке заезда (SCR-003) иконка «Поделиться» в MVP неактивна/декоративна; специфицировать
> поведение шеринга в MVP не требуется. **Согласовано с заказчиком** (2026-06-30, RR-D06). На
> MVP-скоуп и трассировку требований это не влияет.
