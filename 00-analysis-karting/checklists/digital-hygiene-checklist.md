# Чек-лист цифровой гигиены

> Что обязательно проверить руками перед тем, как отдавать артефакты аналитики в разработку
> (Блок 2). Применён к «Апекс» перед сдачей Блока 1.

## Секреты (критично)

- [x] В репозитории **нет секретов**: API-ключи Яндекс.Карт, push-сертификаты APNs/FCM,
      секреты SMS-провайдера, токены, пароли — ни в одном `.md`/`.yaml`/коде.
- [x] `.gitignore` закрывает `.env`, `*.key`, `*.pem`, `secrets/`, `credentials/`,
      `google-services.json`, `GoogleService-Info.plist`, `serviceAccountKey*.json`.
- [x] В OpenAPI-контракте секреты **не описаны** — они параметры окружения (см.
      [`../api/README.md`](../api/README.md)).
- [x] `.env.example` (если появится) содержит только имена переменных без значений.

## Галлюцинации и достоверность

- [x] Нет выдуманных интеграций (сервисы/SDK, которых заказчик не называл): Яндекс.Карты, SMS
      OTP, APNs/FCM, JWT — обоснованы и помечены как проектные решения (R-XXX).
- [x] Нет ссылок на несуществующие стандарты/нормативы (152-ФЗ упомянут корректно как Phase 2).
- [x] Все упомянутые сущности и термины есть в глоссарии (домен + foundations §11).

## Корректность требований

- [x] **ФТ и НФТ не перепутаны** (проверено ревью #1).
- [x] Каждое требование проверяемо (есть критерий приёмки / измеримая метрика).
- [x] У use cases проработаны **альтернативные и исключительные потоки** (UC-1..UC-5).
- [x] Требования не противоречат друг другу и брифу (противоречие таймзоны устранено, К1).
- [x] Все FR покрыты хотя бы одним US/UC (провал обратной трассировки устранён, К2).

## Полнота

- [x] Закрыты все пробелы из брифа (24 вопроса → ответы → требования).
- [x] Явно зафиксировано, что **НЕ входит в MVP** (Phase 2: оценки, share, онлайн-оплата,
      авто-погода, лояльность, подбор карта под рост, публичный рейтинг).
- [x] Граница скоупа (клиент + API; роли маршала/владельца — существующая инфраструктура)
      повторена во всех артефактах.
- [x] Идемпотентность (R-022) и правило 2 часов (FR-17/18) проведены через FR/UC/дизайн/API.

## Согласованность (целостность артефактов)

- [x] Доменное правило картинга (карт всегда центра; места-карты ≠ прокатная экипировка)
      последовательно во всех документах (подтверждено обоими ревью).
- [x] Навигация между экранами сходится (нет тупиков; проверено ревью #2).
- [x] Коды ошибок синхронизированы между матрицей ошибок, UC и OpenAPI.
- [x] Термины единообразны (слот/заезд, место-карт, экипировка/комплект, маршал, конфигурация).

## Документация промптов (требование задания)

- [x] Все промпты ИИ сохранены в [`../prompts/`](../prompts/) (5 файлов по этапам + README).
- [x] Для каждой задачи — артефакт с целью, требованиями, реализацией.

## Передача в разработку (Блок 2)

Передаются: итоговые требования + модель данных + API-спецификация + дизайн-брифы экранов +
ТЗ. См. [`README.md`](../../README.md) корня проекта.
